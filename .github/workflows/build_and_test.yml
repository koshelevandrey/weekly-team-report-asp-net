name: .NET

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      working-directory: ./CM.WeeklyTeamReport
      run: dotnet restore
    - name: Build
      working-directory: ./CM.WeeklyTeamReport
      run: dotnet build --no-restore
    - name: Test
      working-directory: ./CM.WeeklyTeamReport
      run: dotnet test --no-build --verbosity normal --filter FullyQualifiedName\!~IntegrationTests  --collect:"XPlat Code Coverage"
    - name: Codecov upload
      uses: codecov/codecov-action@v2
      
  build-db:
    # The type of runner that the job will run on
    runs-on: windows-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v1.1

      # create dacpac using MSBUILD executable
      - name: create dacpac
        run: msbuild CM.WeeklyTeamReport/src/CM.WeeklyTeamReport.Domain/CM.WeeklyTeamReport.Domain.DB/CM.WeeklyTeamReport.Domain.DB.sqlproj /p:Configuration=Release
      # Publish an artifact
      - uses: actions/upload-artifact@v2
        with:
          name: DACPAC
          path: ${{ github.workspace }}/CM.WeeklyTeamReport/src/CM.WeeklyTeamReport.Domain/CM.WeeklyTeamReport.Domain.DB/bin/Release/
